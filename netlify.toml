[build]
  command = "npm ci --include=optional && npm run build"
  publish = "dist"

[build.environment]
  NPM_FLAGS = "--include=optional"
  NODE_VERSION = "20"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
